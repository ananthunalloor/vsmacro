import * as fs from "fs";

interface PropType {
  [key: string]: string;
}

const testCases: string[] = [];

const generateTestCases = async (componentPath: string): Promise<string[]> => {
  return testCases;
};

export { generateTestCases };
